" =================SYSTEM DEFAULTS DO NOT TOUCH==============
" This line should not be removed as it ensures that various options are
" properly set to work with the Vim-related packages available in Debian.
" Comment out for the Mac version.
runtime! debian.vim

" Source a global configuration file if available
if filereadable("/etc/vim/vimrc.local")
  source /etc/vim/vimrc.local
endif

set nocompatible        " no old school vi compatibility


if has("gui_running")
	colorscheme industry
end

if has("autocmd")
	filetype plugin indent on
endif

syntax on               " Set syntax highlighting
set showcmd		" Show (partial) command in status line.
set showmatch		" Show matching brackets.
set ignorecase		" Do case insensitive matching
set number              " Show line numbers

set tags+=tags;/        " Point vim to CTags files in project root


" fix backspacing problem in Mac OS X
set backspace=indent,eol,start

source after/keybindings.vim

" List of plugins I have installed:
" surround.vim
" matchit.vim
" ruby_matchit.vim
" snipMate.vim
" exuberant-ctags AND taglist.vim
" NERD_tree.vim
" ragtag.vim
" ctrlp.vim
" ack.vim

